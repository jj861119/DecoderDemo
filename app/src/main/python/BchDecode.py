import numpy as np



def BchDecode(secretList):
    # secretArray = np.array(secretList).astype(float)
    # input = ""
    # for i in range(len(secretArray)):
    #     input = input+str(secretArray[i])
    #
    # n = 96
    # my_BCH = bch.BCH(n, 11, 1, bch.get_primitive_polynomial(7))
    #
    # bch = bchlib.BCH(BCH_POLYNOMIAL, BCH_BITS)
    # packet_binary = "".join([str(int(bit)) for bit in secretList[:96]])
    # packet = bytes(int(packet_binary[i : i + 8], 2) for i in range(0, len(packet_binary), 8))
    # packet = bytearray(packet)


    code = ''
    return code
